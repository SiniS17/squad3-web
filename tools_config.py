# ═══════════════════════════════════════════════════════════════
#  Tools page configuration
#  Edit the values below to control the tool table column widths.
#  Use "none" to let a column size automatically to its content.
#  Use CSS length values like "200px", "25vw", etc. to cap a column.
# ═══════════════════════════════════════════════════════════════

TOOLS_TABLE = {
    # Max-width of the Tool Name column  (e.g. "300px" or "none")
    "tool_col_max_width": "none",

    # Max-width of the Part Number column  (e.g. "200px" or "none")
    "pn_col_max_width": "none",

    # Fixed width of the # (row number) column
    "num_col_width": "44px",

    # Fixed width of the Qty column
    "qty_col_width": "90px",
}
